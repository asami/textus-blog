# Spec Directory

Purpose: collect Textus Blog behavior specifications and acceptance-oriented
contracts.

Belongs: operation input/output behavior, import/export contracts, and
application-specific flows that tests or manual validation should satisfy.

Not allowed: exploratory notes, dated handoffs, or broad design rationale that
does not define expected behavior.

Current spec documents:

- `blog-file-tree.md`
