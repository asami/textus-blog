# Textus Blog Docs

This directory follows the CNCF document lifecycle shape at application scope.

- `design/` holds stable application design decisions and runtime policy.
- `spec/` holds behavioral specifications and acceptance-oriented contracts.
- `journal/` holds dated historical notes, handoffs, and retrospectives.

Do not put new application docs at the root unless they are an index for one of
these categories.
